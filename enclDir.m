function encl = enclDir
%Retrieves full path of Enceladus project directory.
%
% SYNOPSIS:
%   encl = enclDir
%
% PARAMETERS:
%   none.
%
% RETURNS:
%   encl - Full path to Enceladus project directory.

% encl = ('/scratch/SAIGUP/DATA/');
encl = ('C:\Users\meisama\Documents\PhD\aPC');
% nm = mfilename('fullpath');
% ix = strfind(nm, filesep);
% if ~isempty(ix),
%    encl = nm(1 : ix(end));
% else
%    encl = nm;
% end
end
